# NOT WORKING!!!
planet_event = { #Add tadpole trait to random enslaved pop without the pop
	id = nice_add_tadpole.1
	is_triggered_only = yes
	hide_window = yes

	trigger = {
		owner = { has_valid_civic = civic_nice_mind_flayer }
	}

	immediate = {
		add_tadpole = yes
	}
}
